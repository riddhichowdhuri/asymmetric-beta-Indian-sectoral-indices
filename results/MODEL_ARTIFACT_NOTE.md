# Research artifact note

This repository was assembled from the supplied notebook, Excel workbook, dashboard HTML, and plot archive.

Two model artifacts are present:

1. **OLS workbook:** contains HC3-robust asymmetric-beta estimates and Wald tests used for `FINDINGS.md`.
2. **ARIMAX notebook/dashboard:** the notebook implements the ARIMAX workflow; the supplied dashboard displays representative/simulated ARIMAX outputs and says so in its source.

No attempt was made to overwrite or reconcile those outputs.
